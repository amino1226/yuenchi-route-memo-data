# yuenchi-route-memo-data

遊園地ルートメモアプリ用のアトラクションデータ配信リポジトリ。

`parks/*.json` はアプリ本体リポジトリ(非公開)の `scripts/export-park-data.ts` から自動生成される。
手動編集しない — 更新はアプリ本体リポジトリの `data/parks/*.ts` を直してエクスポートし直す。

GitHub Pages で配信: https://amino1226.github.io/yuenchi-route-memo-data/parks/<park-id>.json
